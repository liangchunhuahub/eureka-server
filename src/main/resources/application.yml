server:
  port: 8761
spring:
  application:
    name: eureka-serve
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
logging:
  file:
    name: eureka-server.log
  level:
    com:liangchunhua: debug